# homebrew-tap

Homebrew tap for [Thawts](https://github.com/thawts/thawts) — thought capture and review.

## Install

```sh
brew tap thawts/tap
brew install thawts
```

Then run:

```sh
thawts --tui     # terminal UI
thawts --version
```
